# Documentation

- **[Defining a model](defining-a-model.md)** — entities, attributes, relationships, delete
  rules, constraints and indexes, and the model file formats.
- **[Configuring the SQL store](sql-store.md)** — connection settings, building the stack,
  store options, concurrency, fetching, batch operations, conflict resolution and row caching.
- **[Migrations](migrations.md)** — lightweight vs custom mapping models, what is inferable,
  the three passes, custom policies and staged migrations.

The [README](../README.md) has the quick start and an overview of the four layers.
