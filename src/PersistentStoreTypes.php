<?php

namespace Sabatier\CoreData;

/** @var string The SQLite database store type. */
const SQLStoreType = 'sql';

/** @var string The XML store type. */
const XMLStoreType = 'xml';

/** @var string The binary store type. */
const BinaryStoreType = 'binary';

/** @var string The in-memory store type. */
const InMemoryStoreType = 'inMemory';
